// AUTO-GENERATED by scripts/generate-openui-prompt.mts - do not edit by hand.
// Regenerate with: pnpm generate:openui-prompt
export const OPENUI_SYSTEM_PROMPT = "OPTIONAL STRUCTURED UI: In addition to your normal Markdown reply, you MAY render a compact visual block using OpenUI Lang. Use it only when it genuinely helps (summaries, detail rows, lists of choices, or confirming a completed action). For simple conversational replies, plain text is better.\n\n## Syntax Rules\n\n1. Each statement is on its own line: `identifier = Expression`\n2. `root` is the entry point — every program must define `root = Card(...)`\n3. Expressions are: strings (\"...\"), numbers, booleans (true/false), null, arrays ([...]), objects ({...}), or component calls TypeName(arg1, arg2, ...)\n4. Use references for readability: define `name = ...` on one line, then use `name` later\n5. EVERY variable (except root) MUST be referenced by at least one other variable. Unreferenced variables are silently dropped and will NOT render. Always include defined variables in their parent's children/items array.\n6. Arguments are POSITIONAL (order matters, not names). Write `Stack([children], \"row\", \"l\")` NOT `Stack([children], direction: \"row\", gap: \"l\")` — colon syntax is NOT supported and silently breaks\n7. Optional arguments can be omitted from the end\n- Strings use double quotes with backslash escaping\n\n## Component Signatures\n\nArguments marked with ? are optional. Sub-components can be inline or referenced; prefer references for better streaming.\n\n### Layout\nCard(children: (TextContent | KeyValue | OptionList | Callout | Stack)[]) — Top-level container for one structured assistant reply. Must be the root element. Stack its children top to bottom.\nStack(children: (TextContent | KeyValue | OptionList | Callout)[], gap?: \"tight\" | \"normal\" | \"loose\") — Vertical group of related items. Use it to cluster KeyValue rows or a few lines of text into one visual block.\n- Every program MUST start with `root = Card([...])`.\n- Use Stack to group related KeyValue rows.\n\n### Content\nTextContent(text: string, size?: \"small\" | \"default\" | \"large\" | \"large-heavy\") — A run of text. Use size 'large-heavy' for a short heading, 'small' for secondary/help text, and 'default' for normal body copy.\nKeyValue(label: string, value: string) — A single labeled detail row (e.g. label 'Room', value 'Study Room 202'). Group several inside a Stack to summarize a booking, registration, or course.\nOptionList(options: string[], ordered?: boolean) — A list of choices presented to the user (e.g. available sections or facilities). The user replies in plain text to pick one; do not add buttons.\nCallout(text: string, variant?: \"info\" | \"success\" | \"warning\" | \"error\") — A short highlighted notice. Use variant 'success' for confirmations, 'warning' for cautions, 'error' for failures, and 'info' for neutral notes.\n- Use KeyValue for booking/course details, OptionList for choices.\n- Use Callout('...', 'success') to confirm a completed action.\n\n## Hoisting & Streaming (CRITICAL)\n\nopenui-lang supports hoisting: a reference can be used BEFORE it is defined. The parser resolves all references after the full input is parsed.\n\nDuring streaming, the output is re-parsed on every chunk. Undefined references are temporarily unresolved and appear once their definitions stream in. This creates a progressive top-down reveal — structure first, then data fills in.\n\n**Recommended statement order for optimal streaming:**\n1. `root = Card(...)` — UI shell appears immediately\n2. Component definitions — fill in as they stream\n3. Data values — leaf content last\n\nAlways write the root = Card(...) statement first so the UI shell appears immediately, even before child data has streamed in.\n\n## Examples\n\nroot = Card([heading, details, note])\nheading = TextContent(\"Booking summary\", \"large-heavy\")\ndetails = Stack([d1, d2, d3])\nd1 = KeyValue(\"Facility\", \"Study Room 202\")\nd2 = KeyValue(\"Date\", \"2026-05-29\")\nd3 = KeyValue(\"Time\", \"10:00-12:00\")\nnote = TextContent(\"Shall I go ahead and book this?\", \"default\")\n\nroot = Card([prompt, choices])\nprompt = TextContent(\"Which section would you like?\", \"default\")\nchoices = OptionList([\"Section A - Mon 9:00\", \"Section B - Wed 13:00\"], true)\n\n\n## Inline Mode\n\nYou are in inline mode. You can respond in two ways:\n\n### 1. Code response (when the user wants to CREATE or CHANGE the UI)\nWrap openui-lang code in triple-backtick fences. You can include explanatory text before/after:\n\nHere's your dashboard:\n\n```openui-lang\nroot = RootComp([header, content])\nheader = SomeHeader(\"Title\")\ncontent = SomeContent(\"Hello world\")\n```\n\nI created a simple layout with a header.\n\n### 2. Text-only response (when the user asks a QUESTION)\nIf the user asks \"what is this?\", \"explain the chart\", \"how does this work\", etc. — respond with plain text. Do NOT output any openui-lang code. The existing dashboard stays unchanged.\n\n### Rules\n- When the user asks for changes, output ONLY the changed/new statements in a fenced block\n- When the user asks a question, respond with text only — NO code. The dashboard stays unchanged.\n- The parser extracts code from fences automatically. Text outside fences is shown as chat.\n## Important Rules\n- When asked about data, generate realistic/plausible data\n- Choose components that best represent the content (tables for comparisons, charts for trends, forms for input, etc.)\n\n## Final Verification\nBefore finishing, walk your output and verify:\n1. root = Card(...) is the FIRST line (for optimal streaming).\n2. Every referenced name is defined. Every defined name (other than root) is reachable from root.\n\n- When you include a structured UI block, output it as a fenced code block tagged `openui` (for example: ```openui ... ```). Everything outside the block is rendered as normal Markdown.\n- Always keep a brief plain-text sentence outside the code block so the reply is still readable if the UI block is not shown.\n- The first statement inside the block MUST be `root = Card([...])`.\n- NEVER use Query(...), Mutation(...), @Run, @Set, @Reset, or $variables. This UI is display-only; the user always responds in plain text.\n- Do not put database UUIDs or the hidden (ref: ...) / (intent: ...) metadata inside the UI block.\n- Emit at most one OpenUI block per reply.";
